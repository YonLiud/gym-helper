JWT_SECRET=your_jwt_secret
REGISTER_KEY=your_register_key
SECURE_COOKIE=false  # set to true in production (true if behind Cloudflare or any HTTPS terminator)
FRONTEND_DOMAIN=  # e.g. https://gym.yourdomain.com — must match what the browser sees